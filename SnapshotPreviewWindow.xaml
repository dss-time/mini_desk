<Window x:Class="MiniDesk.SnapshotPreviewWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource SnapshotPreview_Title}" Width="650" Height="430" WindowStartupLocation="CenterOwner" Background="{DynamicResource AppBackgroundBrush}" Foreground="{DynamicResource TextBrush}" ResizeMode="NoResize" Language="{DynamicResource CurrentLanguage}">
    <Grid Margin="24"><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
        <TextBlock Text="{Binding Name}" FontSize="24" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" Foreground="{DynamicResource MutedBrush}" Margin="0,5,0,16" Text="{DynamicResource SnapshotPreview_Hint}" />
        <Border Grid.Row="2" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="14">
            <ItemsControl ItemsSource="{Binding Layout.Groups}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate><DataTemplate><Border Width="176" Height="92" Margin="5" CornerRadius="10" Background="#DCEBFF" Padding="12"><StackPanel><TextBlock Text="{Binding Name}" FontWeight="SemiBold" /><TextBlock Foreground="#526887" Margin="0,8,0,0"><Run Text="{Binding ItemCount}" /><Run Text="{DynamicResource Snapshot_IconsSuffix}" /></TextBlock><TextBlock Text="{Binding State}" Foreground="#72809A" FontSize="11" /></StackPanel></Border></DataTemplate></ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
        <Button Grid.Row="3" Content="{DynamicResource Common_Close}" IsDefault="True" Width="90" HorizontalAlignment="Right" Margin="0,16,0,0" Click="Close_Click" />
    </Grid>
</Window>

